import React from 'react';
import './timer.css';

export default class Timer extends React.Component {    
    render() {
        return (
            <div class='timer'>
                the timer component will be here.
            </div>
        );
    }
}